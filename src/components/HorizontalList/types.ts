import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  sectionContainer: {
    marginVertical: 12,
  },
  sectionTitle: {
    color: '#FFFFFF',
    fontSize: 18,
    fontWeight: 'bold',
    marginBottom: 10,
    paddingHorizontal: 16,
  },
  listPadding: {
    paddingHorizontal: 16,
  },
});